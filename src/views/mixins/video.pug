mixin video(video)
  div
    h4
      a(href=`/videos/${video.id}`)=video.title
    p=video.description
    ul 
      each hashtag in video.hashtags
        li=hashtag
    small #{video.views || 0 } #{video.views === 1 ? "view" : "views"}
    br
    small=video.createdAt
    hr